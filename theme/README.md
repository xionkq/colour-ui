# Colour UI Theme

A gradient component theme for Tailwind.

## How to use

Install:

```bash
npm i @xionkq/colour-ui-theme
```

Import in `style.css`:

```css
@import 'tailwindcss';
@import '@xionkq/colour-ui-theme';
```

Use in `index.html`:

```html
<button class="btn">Hello World</button>
```
